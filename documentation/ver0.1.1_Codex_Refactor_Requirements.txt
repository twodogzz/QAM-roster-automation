You are assisting with a refactor of an existing home‑automation project called QAM-docxRoster-to-icsOutlook to a new app called
"QAM‑Roster‑Automation". The goal is to replace a PowerShell-based workflow with a 
clean, modular Python application using the existing Python 3.14 installation in VS Code.
Note: The existing app location: "E:\SoftwareProjects\QAM-docxRoster-to-icsOutlook"

PROJECT PURPOSE:
Automate the import of roster events into Google Calendar. The system must:
1. Parse DOCX roster files.
2. Convert them into structured event objects.
3. Delete previous roster versions from Google Calendar.
4. Insert the new roster events.
5. Maintain auditable logs of all operations.

GOALS:
- Implement Option B: a small, maintainable Python app.
- Use Google Calendar API (OAuth desktop flow).
- Use the existing OAuth client JSON located in:
  E:\SOFTWAREPROJECTS\QAM-ROSTER-AUTOMATION\modules\
  File: credentials.json
- Keep project complexity low to medium.
- Follow the existing SoftwareProjects folder architecture.

PROJECT STRUCTURE (existing):
E:\SOFTWAREPROJECTS\QAM-ROSTER-AUTOMATION
│   .gitignore
│   LICENSE
│   project.json
│   README.md
│
├── documentation\
│       Copilot.rtf
│       Error_Messages.txt
│
├── logs\
│       creation.log
│
├── modules\
│       credentials.json
│
├── scripts\
└── tests\

REQUIREMENTS FOR THE PYTHON REFACTOR:
1. Create a new Python module: calendar_api.py
   - Handles authentication using a local OAuth JSON file.
   - Stores token.json in the project root.
   - Provides functions:
       create_event(event_data)
       delete_events_by_query(query_text)
       update_event(event_id, updated_fields)
       list_events(query_text)
   - Use google-api-python-client and google-auth libraries.

2. Modify or extend qam_docx_to_ics.py (if needed) to output structured event objects 
   instead of ICS files, or create a new parser module.

3. Create main.py to orchestrate:
   - Load roster data.
   - Detect previous version (based on version suffix in event titles).
   - Delete old events.
   - Insert new events.
   - Write detailed logs to logs/creation.log.

4. Ensure all code is modular, readable, and suitable for long-term maintenance.

5. Use Python 3.14 interpreter already configured in VS Code.

6. Follow best practices:
   - Clear function boundaries.
   - Verbose logging.
   - No hard-coded paths (use pathlib).
   - Keep dependencies minimal.

TASK FOR CODEX:
Generate the Python modules, functions, and scaffolding needed to implement this 
refactor. Use the folder structure above. Write clean, well-commented code. 
Focus on practicality and maintainability for a personal home project.
